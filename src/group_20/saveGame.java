public class saveGame extends Save{

    public <board> void saveTheGame(board){
        /*
        parses the board data to a toString() method inherited
        from the abstract save class, it then just commits the
        whole string to the fileName inherited from IO()
         */

    }

    @Override
    public void save() {
        
    }

    public String toString(){
        //converts board data into a string
        return null; //returns null for now because i dont like red lines under my code
    }

}
