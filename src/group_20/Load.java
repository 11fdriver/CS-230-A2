package group_20;
abstract public class Load extends IO{

    public abstract void load();//this needs a more appropriate name
    /*
    abstract load method, works in a similar way to the abstract
    save method in the save class in that it does two different
    things for each subclass but is generally recognised as a
    generic problem that both subclasses need to solve slightly
    differently
     */


}
