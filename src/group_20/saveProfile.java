public class saveProfile extends Save { //gameSetup does not exist yet
    public [String] void saveTheProfiles(){
        /*
        takes the array of profiles, converts them into a string
        and then parses them into a save file in the appropriate
        location in a file, the name of which is derived from the
        fileName attribute inherited from IO()
         */
    }

    @Override
    public void save() {

    }

    public String toString(){
        //converts profile data to a string
        return null; //returns null for now because i dont like red lines under my code
    }
}
